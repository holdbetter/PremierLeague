name: Deploy Backend to Dev Environment

on:
  workflow_dispatch:
  pull_request:
    types: [closed]
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy.yml'

jobs:
  deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image name
        run: echo "IMAGE_BASE=ghcr.io/$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')/backend" >> $GITHUB_ENV

      - name: Build and push via Ktor plugin
        env:
          GHCR_USERNAME: ${{ github.actor }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GHCR_IMAGE: ${{ env.IMAGE_BASE }}:dev
        run: ./gradlew :backend:publishImage

      - name: Tag and push SHA
        run: |
          docker pull ghcr.io/${{ env.IMAGE_BASE }}:dev
          docker tag ghcr.io/${{ env.IMAGE_BASE }}:dev \
                     ghcr.io/${{ env.IMAGE_BASE }}:${{ github.sha }}
          docker push ghcr.io/${{ env.IMAGE_BASE }}:${{ github.sha }}

      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /opt/apps/premier-league-dev
            docker compose pull
            docker compose up -d
            docker image prune -f